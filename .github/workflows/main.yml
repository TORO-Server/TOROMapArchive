name: MapArchive
on:
  schedule:
    - cron: '0 20 * * *' # UTC時間で毎日午前20時 (日本時間 午前5時)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run Archive
        run: |
          docker compose -env GAS_URL=${{ secrets.GAS_URL }} up
          cd map
          ls

permissions:
  contents: write
